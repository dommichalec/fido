<%= link_to current_user.first_name, current_user if current_user%>
<h1>Welcome to Fido!</h1>
<p><%= link_to("Go to active campaigns", campaigns_path)%></p>
<p><%= link_to("Add new campaign", new_campaign_path) %></p>
<% if current_user %>
  <%= link_to "Log out", session_path, method: :delete %>
<% else %>
  <p><%= link_to("Sign up here", sign_up_path) %></p>
  <p><%= link_to("Sign in here", new_session_path) %></p>
<% end %>
